# Kubernetes Secrets Template for Production Environment (app-em-prod)
# 
# Usage:
# 1. Copy this file to k8s/secrets.yaml:
#    cp k8s/secrets-example.yaml k8s/secrets.yaml
# 2. Fill in your actual base64 encoded or string values.
# 3. Apply via: kubectl apply -f k8s/secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: mongodb-atlas-secret
  namespace: app-em-prod
type: Opaque
stringData:
  MONGODB_URI: "mongodb+srv://<username>:<password>@cluster.mongodb.net/employee_db?retryWrites=true&w=majority"
  ENCRYPTION_SECRET_KEY: "<YOUR_32_CHAR_ENCRYPTION_KEY>"
---
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-secret
  namespace: app-em-prod
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "ghcr.io": {
          "username": "<YOUR_GITHUB_USERNAME>",
          "password": "<YOUR_GITHUB_PAT>",
          "auth": "<BASE64_ENCODED_USER_COLON_PAT>"
        }
      }
    }
